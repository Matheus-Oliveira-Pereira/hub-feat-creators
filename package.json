{
  "name": "hub-feat-creators",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9"
  },
  "scripts": {
    "web:dev": "pnpm --filter @hub/web dev",
    "web:build": "pnpm --filter @hub/web build",
    "web:lint": "pnpm --filter @hub/web lint",
    "web:test": "pnpm --filter @hub/web test"
  }
}
